# Copy to .env and fill in.

PORT=8787

# Supabase project (safe to expose the anon/publishable key to the client too -
# that's what it's for; RLS on dynasty_saves is what actually protects data).
SUPABASE_URL=https://jwfakandthizrbvniuvx.supabase.co
SUPABASE_ANON_KEY=sb_publishable_0aeGFFaceNK0H36fauFvBA_0luxwF8Y

# Server-side ONLY. Never expose this to the client. Required for any
# AI-generated narrative turn (Section 9's 4 AI call sites); without it the
# game runs entirely in No AI mode via deterministic fallbacks.
ANTHROPIC_API_KEY=

# Origin(s) allowed to call this API, comma-separated.
CORS_ORIGIN=http://localhost:5173
